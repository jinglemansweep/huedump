{% for idx, l in lights.items() %}
Switch   {{ l._ref }}          "{{ l.name }}"   ({{ l._groups }})          {hue="{{ idx }}"}
Dimmer   {{ l._ref }}_dim      "{{ l.name }}"   ({{ l._groups }})          {hue="{{ idx }};brightness;30"}
{% if l.state.colormode -%}
Colour   {{ l._ref }}_colour   "{{ l.name }}"   ({{ l._groups }})          {hue="{{ idx }}"}
{% endif %}
{%- endfor %}